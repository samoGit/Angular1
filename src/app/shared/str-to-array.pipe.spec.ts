import { StrToArrayPipe } from './str-to-array.pipe';

describe('StrToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new StrToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
